spring:
  ribbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  application:
    name: server-amqp
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 8887
